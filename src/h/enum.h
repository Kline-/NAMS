#ifndef DEC_ENUM_H
#define DEC_ENUM_H

// bitset<MAX_BITSET>    Utils
enum bitsUtils {
    UTILS_DEBUG       = 0,
    UTILS_IGNORE_CASE = 1,
    UTILS_RAW         = 2,
    UTILS_TYPE_ERROR  = 3,
    MAX_UTILS         = 4
};

#endif
